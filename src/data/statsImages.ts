import { ImageSourcePropType } from 'react-native';

// Metro requires string-literal paths, so these are named one by one. Drop a
// new file in under the same name to change the artwork — see the folder's
// README.
export const STATS_HEADER: ImageSourcePropType = require('../../assets/stats/header.jpg');
export const STATS_QUOTE: ImageSourcePropType = require('../../assets/stats/quote.jpg');
